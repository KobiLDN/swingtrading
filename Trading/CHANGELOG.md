# Changelog

Columns: `Date (BST)`, `AI Name`, `Where` (Desktop / Mobile / Web), `Changed` (short description with primary file(s) in backticks).

All notable changes to this project are documented here — **newest first**.

| Date (BST) | AI Name | Where | Changed |
|---|---|---|---|
| 2026-07-20 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **Analysis workflow — null-content guard + model check** — `generate_analysis.py` crashed with `TypeError: expected string or bytes-like object, got 'NoneType'` when OpenRouter returned an empty response body (transient glitch on `deepseek/deepseek-v4-flash`); added guard that raises a descriptive `ValueError` instead of letting `None` propagate into `parse_response`; confirmed `deepseek/deepseek-v4-flash` is live on OpenRouter (5.91T tokens, multiple providers, $0.09/M input); re-triggered workflow — green. (`generate_analysis.py`) |
| 2026-07-20 | Claude Sonnet 4.6 (Claude Code) | Web | Feature: **Liang Wenfeng trader profile** — full card added to `Trading/traders.html` under Quant Pioneer section; stats: 57%+ peak annual gain, $14B AUM, DeepSeek V3 trained for $6M, ~$10B net worth; covers High-Flyer Capital Management (2016, AI-driven quant hedge fund) and DeepSeek (2023 spin-out, R1 release sent Nvidia down 17%); philosophy: consensus mispricing arbitrage, Moore's Law for alpha, open-source as culture; lesson for swing traders on identifying and front-running stale-consensus repricings; sidebar nav + scroll-spy updated; Wikipedia link added. (`Trading/traders.html`) |
| 2026-07-20 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **Trend label uses price position vs EMAs** — `update_prices.py` trend classifier previously only compared EMA50 vs EMA200 (so `e50 > e200` always showed `BULLISH BIAS` even when price was 15% below both EMAs). Expanded to 9 cases: `STRONG UPTREND` (price > e50 > e200), `STRONG DOWNTREND` (price < e50 < e200), `BULLISH BIAS` (e50 > e200 and price above e50, or e50 < e200 and price above e200), `BEARISH BIAS` (e50 > e200 and price below e200, or e50 < e200 and price below e50), `NEUTRAL` (price between the two EMAs in either structure). Prices workflow re-triggered. (`update_prices.py`) |
| 2026-07-20 | Claude Sonnet 4.6 (Claude Code) | Web | UI: **Header brand left-aligned** — removed `margin: 0 auto` from `.header-inner` in `styles/site.css` so the "/ Swing Trading" brand and nav sit at the left edge of the content area, consistent with the left-sidebar dashboard layout. (`styles/site.css`) |
| 2026-07-20 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **News feed — Alpha Vantage → Yahoo Finance RSS; calendar window widened** — Alpha Vantage `NEWS_SENTIMENT` endpoint returning 403 (free-tier API key revoked or rate-limited); `update_news.py` rewritten to pull Yahoo Finance RSS feeds for each asset ticker (no API key required, always populated); asset-ticker map: GBP/USD→GBPUSD=X, EUR/USD→EURUSD=X, XAU/USD→GC=F, Silver→SLV, SPX→SPY, WTI→USO; news card footer updated to "Powered by Yahoo Finance RSS". Also: `update_calendar.py` week-window widened — was Monday-gated (empty Thu–Sun); now always fetches current week Mon–Sun plus next week Mon–Sun regardless of day. (`update_news.py`, `update_calendar.py`, `index.html`) |
| 2026-07-03 | Claude Sonnet 5 (Claude Code) | Web | Fix + Docs: **Silver proxy fix + STRICT branch rules** — `XAG/USD` also 404s on Twelve Data free tier (same issue as WTI); switched to `SLV` (iShares Silver Trust) ETF proxy in `update_prices.py`, `generate_analysis.py`, both workflows, and `index.html` asset context card ("Silver (via SLV)"). Also: `Trading/BRANCHES.md` rewritten with two strict rules — all work goes to the dev branch first and gets checked on its Cloudflare preview before anything touches live; pushing to `main` always requires asking Kobi first; same rules added to top of `Trading/AGENTS.md`. Bot-only exception: GitHub Actions daily data commits still land on main directly. (`update_prices.py`, `generate_analysis.py`, `index.html`, `Trading/BRANCHES.md`, `Trading/AGENTS.md`) |
| 2026-07-02 | Claude Fable 5 (Claude Code) | Web | Feature: **EUR/USD + XAG/USD (Silver) as 5th and 6th assets** — direct Twelve Data symbols, no proxies. `update_prices.py` + `generate_analysis.py` configs, both workflows gain fetch/analyse steps, `update_news.py` fetches `FOREX:EURUSD` + `FOREX:XAGUSD`. `index.html`: sidebar pills (ordered forex → metals → index → energy), asset context cards (EUR/USD "Fiber" with ECB/Fed drivers; Silver with industrial demand + gold/silver ratio), calendar explainers per asset, placeholder JS files. Also today: WTI oil fixed — `USOIL` and `WTI/USD` both 404 on Twelve Data free tier, switched to `USO` ETF proxy (same pattern as SPY). (`update_prices.py`, `generate_analysis.py`, `update_news.py`, both workflows, `index.html`) |
| 2026-06-15 | Claude Sonnet 4.6 (Claude Code) | Web | Infra: **Branch cleanup + project docs** — removed redundant `dev` branch (was a mirror of main with no purpose); two branches now: `main` (live) + `claude/friendly-dijkstra-lBUrd` (cloud session). Created `Trading/BRANCHES.md` (branch table, push sequence) and `Trading/KANBAN.md` (backlog / todo / in progress / done table with owner and date per item). (`Trading/BRANCHES.md`, `Trading/KANBAN.md`) |
| 2026-06-14 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **Traders sidebar scroll — name no longer cut off by sticky header** — added `scroll-margin-top: 74px` to `.trader-card` so clicking a name in the sidebar scrolls to the card with 74px clearance above it (header is 58px). (`Trading/traders.html`) |
| 2026-06-14 | Claude Sonnet 4.6 (Claude Code) | Web | UI: **Price Overview 3-col + Candlestick horizontal + Pattern Guide** — (1) Price Overview section now a 3-column `overview-grid`: existing price/ATR/EMA/trend card, new "About This Asset" card (per-asset name, description, key drivers, session hours, pip value — populated by `ASSET_CONTEXT` JS object, switches with asset pill via `updateAssetContext()`), new "AI Quick Verdict" mini card (BUY/SELL/WAIT in colour, confidence, entry, stop loss, target 1, R/R — populated by `updateVerdictMini()` called from `renderAnalysis()`). (2) Candlestick Patterns list changed from column to `flex-wrap` so patterns sit side by side at 180px min-width. (3) Full-width Pattern Guide card added below patterns explaining all 9 patterns (Hammer, Shooting Star, Bullish/Bearish Engulfing, Doji, Morning Star, Evening Star, Bullish/Bearish Harami) in auto-fit grid. (`index.html`) |
| 2026-06-09 | Claude Sonnet 4.6 (Claude Code) | Web | UI: **Inline explainer panels + 1440px layout + enriched AI prompt** — (1) Three full-width explainer cards added to dashboard: chart lines (EMA 50/200, Golden Cross, S/R), indicator guide (RSI, divergence, MACD, signal score /10), and "What moves this asset" (calendar context). (2) Calendar + indicator explainers use horizontal auto-fit grid — no side columns, no gaps. (3) "What moves" card switches content per asset: GBP/USD shows CPI/ECB/GDP context; XAU shows real yields/FOMC/NFP/DXY/geopolitical; SPX shows FOMC/CPI/NFP/GDP/earnings. (4) Site widened from 1200px to 1440px across all 3 pages for 1080p. (5) Copy AI Prompt expanded to 5 sections: technical indicators, existing AI verdict (DeepSeek decision/entry/stop/targets/invalidation), upcoming calendar events with forecast + previous, news sentiment score + top 3 headlines, task instructions referencing all context. (`index.html`, `Trading/encyclopedia.html`, `Trading/traders.html`) |
| 2026-06-09 | Claude Sonnet 4.6 (Claude Code) | Web | Infra: **Dual-source economic calendar (Forex Factory + Twelve Data)** — `update_calendar.py` rewritten to fetch from both sources and merge; Forex Factory JSON (`ff_calendar_thisweek.json` + `ff_calendar_nextweek.json`) is primary — no API key, always populated; Twelve Data is supplementary — adds any events not already in FF; dedup by normalised (date, currency, title prefix); `nextweek.json` 404 handled gracefully (FF publishes it near weekend); calendar dashboard card now reliably shows events every week (`update_calendar.py`, `calendar-data.js`) |
| 2026-06-09 | Claude Sonnet 4.6 (Claude Code) | Web | Feature: **Alpha Vantage news sentiment feed — Step 4 complete** — `update_news.py` fetches 8 latest articles + per-ticker sentiment score for GBP/USD (`FOREX:GBPUSD`), XAU/USD (`FOREX:XAUUSD`), SPX (`EQUITY:SPY`) from Alpha Vantage `NEWS_SENTIMENT` endpoint; writes `news-data.js` → `window.NEWS_DATA`; `generate-analysis.yml` runs script daily at 06:10 UTC using `ALPHA_VANTAGE_API_KEY` secret, commits `news-data.js`; placeholder file prevents 404 before first run; `index.html` news card renders below economic calendar — per-asset (switches with pill), Bullish/Bearish/Neutral badge, sentiment score, source, timestamp, summary excerpt, clickable headline link; hidden when no data (`update_news.py`, `news-data.js`, `.github/workflows/generate-analysis.yml`, `index.html`) |
| 2026-06-09 | Claude Sonnet 4.6 (Claude Code) | Web | Docs: **FEATURES.md + CHANGELOG.md synced** — docs updated to reflect all local overnight work: Steps 1–3 and Step 5 marked done, SPX→SPY fix, shared CSS, encyclopedia search/collapsible sidebar, traders sidebar + Wikipedia links, Sims integration, economic calendar, backtesting script; FEATURES.md backlog trimmed to Step 4 only (`Trading/FEATURES.md`, `Trading/CHANGELOG.md`) |
| 2026-06-07 | Claude Sonnet 4.6 (Claude Code) | Web | Infra: **Sims multi-agent integration** — `generate_analysis.py` gains `build_sims_topic()`: after each daily AI analysis, writes a one-paragraph debate prompt (`sims/topic-{slug}.txt`) containing signal score, AI verdict (BUY/SELL/WAIT), entry/stop/targets, R/R, price vs EMAs, ATR, RSI, MACD direction, detected patterns, and invalidation — ending with a challenge question for multi-agent debate. Workflow `git add` updated to include `sims/topic*.txt` so files are committed and available at raw GitHub URLs. All Sims output lives under `sims/` subfolder (clean separation from root). (`generate_analysis.py`, `.github/workflows/generate-analysis.yml`) |
| 2026-06-06 | Claude Sonnet 4.6 (Claude Code) | Web | UI: **Shared CSS + header fix + traders sidebar + encyclopedia enhancements** — (1) `styles/site.css` created — single source of truth for all header/nav CSS; all 3 pages link to it, eliminating copy-paste inconsistency and drift. (2) Fixed encyclopedia.html header bounce: removed `justify-content:space-between` and hardcoded `max-width:1160px` (was 40px narrower than other pages), standardised `theme-btn` padding/font-size across all pages. (3) SPX data fix: `SPX500USD` not valid on Twelve Data free tier — switched to `SPY` (S&P 500 ETF, 99.9%+ correlation) in `update_prices.py`, `generate_analysis.py`, and both workflow files. (4) `Trading/encyclopedia.html` — full-width sticky search bar (`/` to focus, ESC to clear, amber highlight, match count); EMA 50/200 section expanded for complete beginners (formula with k=2/(n+1), SMA vs EMA comparison, weight cards, time-context cards, self-fulfilling prophecy explanation); collapsible sidebar sections (Books and Momentum — click to expand/collapse with chevron animation); sidebar sub-nav items for RSI(14), MACD(12,26,9), EMA 50/200, ATR(14), AVWAP under Momentum; scroll spy updated for all sub-heading IDs. (5) `Trading/traders.html` — sticky sidebar replaces jump-chip grid (10 traders grouped: Quant / Macro / Technical / Value / Trend with colour dots matching card border colours, scroll spy highlights active trader); Wikipedia link on every trader card (opens in new tab); Back to top button in sidebar. (`styles/site.css`, `index.html`, `Trading/encyclopedia.html`, `Trading/traders.html`) |
| 2026-06-06 | Claude Sonnet 4.6 (Claude Code) | Web | Feature: **Multi-asset pipeline + economic calendar + backtesting** — (1) `update_prices.py` rewritten with `ASSET` env var support; writes `prices-data-{slug}.js` + `window.PRICES_DATA_{SLUG}` per asset (GBP/USD, XAU/USD, SPX500USD); asset config adds `pip_mult`, `pip_label`, `pip_value`, `decimals`, `slug` fields; backward compat legacy files still written for GBP/USD. (2) `generate_analysis.py` updated to read `prices-{slug}.json`, write `analysis-data-{slug}.js` per asset. (3) `update_calendar.py` new script — Twelve Data `/economic_calendar` endpoint, filters USD/GBP/EUR high-impact, writes `calendar-data.js`. (4) Both GitHub Actions workflows updated to loop over all 3 assets sequentially + run calendar script. (5) `index.html` — asset selector pills in header (GBP/USD · XAU/USD · SPX), `switchAsset()` swaps globals + re-renders, calendar warning banner for today's events, 7-day events table card, ATR label dynamic (pips/pts), chart title dynamic, position calculator uses `pip_value` from data, `buildPrompt` uses `d.symbol` + `d.pip_label`. (6) Placeholder JS files committed so page doesn't 404 before first Action run. (7) `Trading/backtest.py` — new standalone script; fetches 730 daily GBP/USD candles, runs all 9 pattern detectors across full history, measures outcomes at 5/10/20 candle horizons, classifies each signal by regime (Trending ↑/↓, Consolidating, Ranging), outputs `Trading/backtesting.md` with 6-section report (by pattern, by hold period, by regime, pattern×regime matrix, year-by-year, key findings). (8) `Trading/AGENTS.md` updated — multi-asset config table, calendar and backtesting sections added, pending work updated to show all 5 steps done (`update_prices.py`, `generate_analysis.py`, `update_calendar.py`, `.github/workflows/update-prices.yml`, `.github/workflows/generate-analysis.yml`, `index.html`, `Trading/backtest.py`, `Trading/AGENTS.md`). |
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | Docs: **FEATURES.md + CHANGELOG.md updated** — backlog restructured into 5 numbered steps (multi-asset pipeline, asset selector, dashboard widgets, calendar/news, backtesting); done section adds encyclopedia HTML and Ariyan advice entries (`Trading/FEATURES.md`, `Trading/CHANGELOG.md`). |
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | Docs: **encyclopedia expanded (Ariyan's advice)** — VWAP/Anchored VWAP sub-section (with daily vs intraday note); LAGGING badges on RSI, MACD, EMA headings; Section 10 Market Regime (strong uptrend/downtrend/weak trend/consolidation table, regime filter rule, variable isolation guidance for backtesting); Section 11 Risk Management (1–2% rule, position size formula, worked GBP/USD example, risk rules table); sidebar nav + scroll spy updated for sections 10 and 11 (`Trading/encyclopedia.md`, `Trading/encyclopedia.html`). |
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | Docs: **encyclopedia.html sidebar + books section** — replaced horizontal pill nav with fixed left sidebar (188px, sticky, 11 sections); active link highlights via IntersectionObserver scroll spy; smooth scroll with header offset; Section 9 labelled "Books"; mobile hides sidebar below 780px (`Trading/encyclopedia.html`). |
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | Docs: **encyclopedia.html created and synced** — full styled HTML version of encyclopedia.md pushed to main; MACD 3-component explanation + histogram shrinking row; EMA plain-English paragraph; ATR sub-section; weekend gap risk row in GBP/USD table; Section 9 recommended reading (12 traders table, Market Wizards series, trader archetypes) (`Trading/encyclopedia.html`). |
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | Dashboard: **beginner-friendly context pass** — collapsible "How to use" card (4-step guide: score thresholds, R/R meaning, encyclopedia + prompt workflow, trade logging); ATR label updated to "typical daily candle size · use for stop loss sizing"; trend badge now shows EMA criteria below it (e.g. `Price > EMA 50 > EMA 200`); signal score card adds breakdown table showing pattern/RSI/MACD histogram points; pattern checklist now shows signal direction (e.g. "BULLISH REVERSAL") alongside date and strength; R/R cell renamed "Risk / Reward" with "risk 1 to make X" sub-note; Confidence cell adds contextual sub-label (wait for confirmation / moderate conviction / strong conviction); copy-prompt instructions updated with step-by-step workflow; `macd_hist_improving` field added to `update_prices.py` payload and JS `calcAllIndicators` return object for score breakdown (`index.html`, `update_prices.py`). |
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | Docs: **encyclopedia Section 9 — recommended reading** — 12 traders/authors with books and credentials; Market Wizards series table; trader archetypes breakdown (macro swing, technical/mechanical, momentum/breakout, psychology, beginner foundation) (`Trading/encyclopedia.md`). |
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | Docs: **README + encyclopedia updated** — README adds browser calculator as primary no-setup option above Python CLI; encyclopedia Section 4 adds plain-English "what it is" explanations for RSI, MACD (with component breakdown), EMA 50/200, and ATR before existing threshold tables (`README.md`, `Trading/encyclopedia.md`). |
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | Dashboard: **candlestick chart + pattern checklist + in-browser OHLC calculator** — Lightweight Charts v4 candlestick chart with EMA 50/200 overlay and dashed AI S/R price lines; 9-pattern checklist with ✓/✗ replacing blank "none detected" state; full JS port of `update_prices.py` indicator logic (EMA, RSI Wilder, MACD, ATR, all 9 pattern detectors, RSI divergence, scoring) runs entirely in-browser on pasted OHLC JSON (Twelve Data / Alpha Vantage / plain array); `generate_analysis.py` prompt + parser updated to extract `SUPPORT_LEVELS` / `RESISTANCE_LEVELS` arrays into `analysis-data.js`; `AGENTS.md` + `FEATURES.md` updated to reflect current state (`index.html`, `generate_analysis.py`, `Trading/AGENTS.md`, `Trading/FEATURES.md`). |
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | Infra: **automated pipeline live** — `update_prices.py` fetches 200 daily OHLC candles from Twelve Data, calculates indicators, writes `prices-data.js` + `prices.json`; `generate_analysis.py` calls OpenRouter (DeepSeek v4 Flash), parses structured response, writes `analysis-data.js` + `last_analysis.md`; GitHub Actions workflows run Mon–Fri at 06:00 and 06:10 UTC; `index.html` live dashboard on GitHub Pages; `README.md` updated with full project structure and setup guide (`update_prices.py`, `generate_analysis.py`, `index.html`, `.github/workflows/update-prices.yml`, `.github/workflows/generate-analysis.yml`, `README.md`). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Infra: **Python OHLC calculator** — `ohlc_calculator.py` calculates RSI(14) Wilder smoothing, MACD(12,26,9), EMA(50/200), ATR(14); detects 9 candlestick patterns on last 5 candles; RSI divergence check over 20-candle lookback; 0-10 signal scoring per encyclopedia rules; outputs formatted analysis + pre-filled AI prompt block to console and `analysis_output.txt`; supports Twelve Data + Alpha Vantage JSON; `ohlc_data.json` sample data; `run_calculator.bat` double-click runner; `requirements.txt` (`ohlc_calculator.py`, `ohlc_data.json`, `run_calculator.bat`, `requirements.txt`). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Docs: **prompt_template.md + trade_log.md created** — 3 prompt variants (quick scan, full deep analysis, entry/exit only) with tips table; trade log with active/closed tables, setup key, outcome key, monthly summary, and logging rules (`prompt_template.md`, `trade_log.md`). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Docs: **AGENTS.md + CHANGELOG.md + FEATURES.md created** — full project documentation added; AGENTS.md covers project purpose, file structure, workflow, scoring system, data sources, and expansion guide; FEATURES.md seeds backlog with Python auto-calculator, EUR/USD pair, HTML dashboard, and Claude API automation (`AGENTS.md`, `CHANGELOG.md`, `FEATURES.md`). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | **Initial knowledge base created** — `index.md` (dashboard: OHLC data input template, AI analysis prompt, quick reference card, last analysis placeholder, resource links) + `encyclopedia.md` (full pattern recognition guide: 9 candlestick patterns, 13 chart patterns, S/R rules, RSI/MACD/EMA indicators, volume confirmation, GBP/USD characteristics, 0-10 scoring system, Python pseudocode); content sourced from DeepSeek (`index.md`, `encyclopedia.md`). |
