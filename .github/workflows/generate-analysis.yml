name: Generate AI Analysis

on:
  schedule:
    # 06:10 UTC daily — 10 min after update-prices
    - cron: '10 6 * * 1-5'
  workflow_dispatch:

jobs:
  analyse:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Pull latest so we get the prices files just committed by update-prices
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests python-dotenv

      - name: Analyse GBP/USD
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          ASSET: "GBP/USD"
        run: python generate_analysis.py

      - name: Analyse XAU/USD
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          ASSET: "XAU/USD"
        run: python generate_analysis.py || echo "XAU/USD analysis failed — skipping"

      - name: Analyse SPX
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          ASSET: "SPY"
        run: python generate_analysis.py || echo "SPY analysis failed — skipping"

      - name: Commit analysis output
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add analysis-data*.js last_analysis*.md sims_topic*.txt
          git diff --cached --quiet || git commit -m "Update AI analysis — $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push origin main
