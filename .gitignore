# Local calculator output — re-created on each run, not needed in repo
Trading/analysis_output.txt

# prices-data.js, analysis-data.js, prices.json, last_analysis.md are committed
# (they are the live site's data files, auto-updated by GitHub Actions)

# Claude Code local settings
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# OS
Thumbs.db
.DS_Store
desktop.ini
